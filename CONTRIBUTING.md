# Contributing

Follow the family-root `AGENTS.md`, preserve member parity, and run `python3 tools/family-check.py`
before review.

Every push to `main` that changes a family member must update `CHANGELOG.md` under `[Unreleased]`.
