---
id: <<store_slug>>.operating-rules.TODO-rule-slug
name: "TODO: rule name"
type: doctrine
layer: C3
status: proposed
load: triggered
owner: shared
created: "{{YYYY-MM-DD}}"
tags: [operating-rules, proposed]
---

# TODO: rule name

> Copy this file, fill it, propose it (`status: proposed`), collect roster sign-off per
> `_meta/governance.md`, then flip to `status: current` and add the row to the README table.
> Follow `rule-plumbing.md` - all six steps.

## The rule

<!-- One paragraph. If it takes three, it is probably two rules. -->

## When this applies

<!-- The trigger, precise enough that two agents agree whether a situation is covered. -->

## Why

<!-- The failure this rule prevents - ideally the incident that motivated it. -->

## What it does not cover

<!-- The nearest out-of-scope cases, so the rule does not creep. -->
